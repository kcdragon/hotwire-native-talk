# Intentionally empty. The deck uses Slidev hash routing (routerMode: hash),
# so every request resolves to index.html client-side and no SPA rewrite is
# needed. This file exists only to stop `slidev build` from generating a
# `/*  /index.html  200` rule, which Cloudflare Pages rejects as an infinite
# loop. Do not add rewrite rules here.
